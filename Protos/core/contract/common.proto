syntax = "proto3";

package protocol;

option csharp_namespace = "Simple.Chain.Protocol";
option go_package = "github.com/tronprotocol/grpc-gateway/core";

enum ResourceCode {
  BANDWIDTH = 0x00;
  ENERGY = 0x01;
}